import React                from 'react'


import Login                from './Login'

const title = `Login`

export default {
  path: '/login',

  action() {
    return {
      title,
      component: <Login title={ title } />,
    }
  },
}
